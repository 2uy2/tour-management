extends ../../layouts/default.pug 
include ../../mixins/box-head.pug 

block main 
    .container.my-3 
        .row
            .col-12
                +box-head(pageTitle)
            
        .row 
            each item in categories
                .col-6.mb-3 
                    div(class="card")
                        img(src=item.image, alt="")
                        div(class="card-body")
                            h5(class="card_title") #{item.title}
                            a(href=`/tours/${item.slug}` class="btn btn-primary") xem chi tiết 
                